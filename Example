setwd("~/Desktop") ### set working directory, for example I saved the Data.csv in my Desktop
data <- read.csv("Data.csv") ### read csv data
attach(data)
X <- cbind(X1,X2)
y <- test.result
c <- observed.time

RegPO_MCSD(X=X,y=y,c=c,alpha=0.9,beta=0.9,m.knots=6)


Under editing...







